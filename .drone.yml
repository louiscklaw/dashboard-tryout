kind: pipeline
name: deploy_firebase
type: docker

steps:
  - name: deploy_firebase
    image: yarnpkg/node-yarn
    commands:
      - cd docusaurus-dashboard
      - yarn
      - yarn build
      - yarn deploy_firebase
